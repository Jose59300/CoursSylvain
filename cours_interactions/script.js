//ecouteurs : element qui va attendre le moment pour jouer l'evenement
/*document.getElementById('button').addEventListener('click',function(){
  alert('ok')
});*/
// le mot clé 'this' est une référence au champ a laquelle je vais utiliser un ecouteur
console.log(document.querySelector('.container').getBoundingClientRect());
// La méthode Element.getBoundingClientRect() renvoie la taille d'un élément et sa position relative par rapport à la zone d'affichage (viewport).
